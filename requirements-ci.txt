# Minimal deps to run the hermetic unit tests in CI — data-quality + scoring +
# consensus. Deliberately excludes the genomics/ML stack (scanpy, anndata, scipy,
# torch-adjacent) and browser tooling; those tests import only the API/db layer.
pytest==9.1.1
numpy==2.2.6
great-expectations==1.21.0
fastapi==0.141.1
pydantic[email]==2.10.6
pydantic-settings==2.15.0
httpx==0.28.1
supabase==2.15.2
python-multipart==0.0.32
resend==2.30.1
